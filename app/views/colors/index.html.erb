<h1>Listing colors</h1>

<table>
  <tr>
    <th>Hexa</th>
    <th>Description</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @colors.each do |color| %>
  <tr>
    <td><%= color.hexa %></td>
    <td><%= color.description %></td>
    <td><%= color.user %></td>
    <td><%= link_to 'Show', [color.user, color] %></td>
    <td><%= link_to 'Edit', edit_user_color_path(color.user, color) %></td>
    <td><%= link_to 'Destroy', [color.user, color], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Color', new_user_color_path(params[:user_id]) %>
